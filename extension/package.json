{
  "name": "tokenomics-web-companion",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Tokenomics browser companion — AI usage at a glance.",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "test": "node build-tests.mjs && node --test 'dist-test/**/*.test.mjs'"
  },
  "dependencies": {
    "preact": "^10.24.0",
    "webextension-polyfill": "^0.12.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "@types/node": "^25.8.0",
    "@types/webextension-polyfill": "^0.12.1",
    "esbuild": "^0.28.1",
    "typescript": "^5.6.0"
  }
}
